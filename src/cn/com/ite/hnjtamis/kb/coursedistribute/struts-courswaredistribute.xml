<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
    <package name="coursewaredistribute" extends="eap-default" namespace="/kb/coursedistribute">
       <action name="*For*Action" class="{2}Action" method="{1}">
            <result type="json" name="list">
                <!-- 这个属性表示要包含进JSON数据中的数据(listAttachment\[\d+\]\.realName对指定的对象属性json化)-->
                <param name="includeProperties">
                total,list\[\d+\]\.[A-Za-z0-9_]+,
                list\[\d+\]\.organ.organId,list\[\d+\]\.organ.organName,
                list\[\d+\]\.courseware.[A-Za-z0-9_]+,
                list\[\d+\]\.quarter.quarterId,
                list\[\d+\]\.quarter.quarterName,
                list\[\d+\]\.speciality.specialityid,
                list\[\d+\]\.speciality.specialityname
                </param>  
                <!-- 这个属性表示不要包含进JSON数据中的数据。 
                <param name="excludeProperties">list\[\d+\]\.accessories</param> -->
                <!-- 这个属性表示去除空数据。 -->
                <param name="excludeNullProperties">true</param>
                <!-- ignoreHierarchy这个属性表示是否忽略等级,也就是继承关系,设置为 false后会将父类和子类的属性一起返回 -->
                <param name="ignoreHierarchy">false</param> 
            </result>
            <result type="json" name="delete">
                <param name="includeProperties">msg,success</param>
                <param name="excludeNullProperties">true</param>
                <param name="ignoreHierarchy">false</param> 
            </result>
            <result type="json" name="find">
            	<!-- <param name="root">form</param>-->
                <param name="includeProperties">
                	form\.[A-Za-z0-9_]+,form.organ.organId,form.organ.organName,
                	form.courseware\.[A-Za-z0-9_]+,
                	form.quarter\.[A-Za-z0-9_]+,
                	form.speciality\.[A-Za-z0-9_]+
                </param>
                <!--<param name="excludeProperties"></param> -->
                <param name="excludeNullProperties">true</param>
                <param name="ignoreHierarchy">false</param> 
            </result>
            <result type="json" name="findDistributeTree">
                <param name="root">children</param>
                <param name="excludeNullProperties">true</param>
                <param name="ignoreHierarchy">false</param> 
            </result>
            <result type="json" name="save">
                <param name="contentType">text/html</param>
                <param name="includeProperties">msg,success</param>
                <param name="excludeNullProperties">true</param>
                <param name="ignoreHierarchy">false</param> 
            </result>
       </action>
    </package>
</struts>